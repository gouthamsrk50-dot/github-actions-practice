# name: Hello Workflow

# on:
#   workflow_dispatch:

# jobs:
#   hello:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Print Hello
#         run: echo "Hello Goutham! Welcome to GitHub Actions."

name: Hello Workflow

on:
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print Multiple Messages
        run: |
          echo "Hello Goutham!"
          echo "Learning GitHub Actions..."
          echo "This workflow is running on Ubuntu."